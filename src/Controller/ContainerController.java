package Controller;
import Model.Container;
import java.util.ArrayList;
import java.util.Queue;
public class ContainerController {
    private Queue<Container> cola = new Queue<Container>();

    /*public void create(Container n1) {
        container.push(n1);
    }

    public ArrayList<String[]> Read(String filter) {
        ArrayList<String[]> Get = new ArrayList<>();
        for (int i = 0; i < container.size(); i++) {
            Get.add(new String[]{});
        }
        return Get;
    }


    public void Update(int index, Container n1) {
        container.set(index, n1);
    }

    public void Delete() {
        container.pop();
    }*/

}
